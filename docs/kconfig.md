# `kconfig` In `ktools-rust`

`ktools-rust/` does not currently include a checked-in `kconfig`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `kconfig` is not yet implemented in the current Rust workspace
- this page exists so the website can stay complete without inventing
  website-only status text
