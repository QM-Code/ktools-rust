# `ki18n` In `ktools-rust`

`ktools-rust/` does not currently include a checked-in `ki18n`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `ki18n` is not yet implemented in the current Rust workspace
- future Rust docs should replace this status markdown directly
