# Beta Demo SDK

Exists for CI and as a minimal reference for integrating an SDK add-on with the
Rust `ktrace` crate.

Like alpha, this SDK is a reference for the `get_trace_logger()` integration
pattern used by libraries that want to expose trace channels to executables.
