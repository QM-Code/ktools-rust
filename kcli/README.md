# kcli-rust

`ktools-rust/kcli/` is the Rust implementation of `kcli`.

## Build

From this repo root:

```bash
./kbuild.py --build-latest
```

Direct Cargo workflow:

```bash
cd src
cargo test
```

## Demos

The local Rust `kbuild` config stages:

- `demo/exe/core/build/latest/`
- `demo/exe/omega/build/latest/`
